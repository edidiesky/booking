import Head from "next/head";
import { Work_Sans, Bebas_Neue, Karla } from "next/font/google";
const karla = Karla({
  subsets: ["latin"],
  variable: "--font-karla",
  display: "swap",
  weight: ["400", "500", "600", "700"],
});
import Inner from "@/components/Layout/Inner";
import HomeIndex from "./_components";
export default function Payment() {
  return (
    <div className={`${karla.variable}`}>
      <Head>
        <title>Reservation Details | Home | Payment </title>
        <meta name="description" content="Generated by create next app" />Marvin
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="https://hopper.com/favicon.ico" />
      </Head>
      <Inner>
        <HomeIndex />
      </Inner>
    </div>
  );
}
