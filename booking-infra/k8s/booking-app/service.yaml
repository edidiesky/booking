apiVersion: v1
kind: Service
metadata:
  name: booking
  namespace: booking-platform
spec:
  type: NodePort
  selector:
    app: booking
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
